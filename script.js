const alignmentStyles=document.createElement('link');alignmentStyles.rel='stylesheet';alignmentStyles.href='./alignment-fix.css?v=1';document.head.appendChild(alignmentStyles);const menuToggle=document.querySelector('.menu-toggle');const navLinks=document.querySelector('.nav-links');menuToggle?.addEventListener('click',()=>{const open=navLinks.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>navLinks.classList.remove('open')));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const card=document.querySelector('.growth-card');if(card&&window.matchMedia('(pointer:fine)').matches){document.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;card.style.transform=`rotateX(${(-y*4).toFixed(2)}deg) rotateY(${(x*5).toFixed(2)}deg) rotateZ(1deg)`});card.addEventListener('pointerleave',()=>card.style.transform='rotate(3deg)')};const form=document.getElementById('contact-form');const status=document.getElementById('form-status');form?.addEventListener('submit',e=>{e.preventDefault();const submitter=e.submitter;const data=new FormData(form);const name=(data.get('name')||'').trim();const business=(data.get('business')||'').trim();const phone=(data.get('phone')||'').trim();const service=data.get('service')||'';const message=(data.get('message')||'').trim();const plain=`New Loopify enquiry\n\nName: ${name||'Not provided'}\nBusiness: ${business||'Not provided'}\nPhone: ${phone||'Not provided'}\nService: ${service}\nMessage: ${message||'Not provided'}`;if(submitter?.value==='email'){const subject=encodeURIComponent(`New Loopify enquiry - ${name||'Website lead'}`);window.location.href=`mailto:loopifydm@gmail.com?subject=${subject}&body=${encodeURIComponent(plain)}`;status.textContent='Opening your email app…'}else{window.open(`https://wa.me/919360931245?text=${encodeURIComponent(plain)}`,'_blank','noopener');status.textContent='Opening WhatsApp…'}if(window.gtag)window.gtag('event','generate_lead',{method:submitter?.value||'website_form',service});if(window.fbq)window.fbq('track','Lead');form.reset()});